<section class="pillars" aria-labelledby="pillars-title" id="requests">
  <div class="pillars__inner">
    <p class="pillars__eyebrow">Что можно увидеть в карте</p>

    <div class="pillars__grid">
      <article class="pillar">
        <h3>Любовь</h3>
        <p>Отношения, партнёр, семья</p>
      </article>

      <article class="pillar">
        <h3>Финансы</h3>
        <p>Доход, карьера, реализация</p>
      </article>

      <article class="pillar">
        <h3>Судьба</h3>
        <p>Таланты, путь, предназначение</p>
      </article>

      <article class="pillar">
        <h3>Энергия</h3>
        <p>Ресурс, здоровье, внутренний ритм</p>
      </article>
    </div>
  </div>
</section>

<style>
  .pillars {
    position: relative;
    background: #fff;
    padding: clamp(72px, 8vw, 120px) 0;
  }

  .pillars__inner {
    width: min(1440px, calc(100% - 64px));
    margin: 0 auto;
  }

  .pillars__eyebrow {
    margin: 0 0 34px;
    text-align: center;
    font-size: 12px;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: rgba(31, 29, 27, 0.45);
  }

  .pillars__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1px;
    background: rgba(31, 29, 27, 0.08);
  }

  .pillar {
    background: #fff;
    padding: clamp(34px, 4vw, 54px);
    text-align: center;
    transition: all .35s ease;
  }

  .pillar:hover {
    background: #faf8f6;
  }

  .pillar h3 {
    margin: 0;
    font-family: var(--font-serif);
    font-size: clamp(44px, 4vw, 72px);
    font-weight: 400;
    line-height: .9;
    letter-spacing: -0.05em;
  }

  .pillar p {
    margin: 18px auto 0;
    max-width: 180px;
    color: rgba(31,29,27,.58);
    font-size: 14px;
    line-height: 1.5;
  }
.pillar h3 {
  transition: color 0.3s ease;
}

.pillar:hover h3 {
  color: var(--accent);
}


  @media (max-width: 900px) {
    .pillars__grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .pillar {
      padding: 32px 24px;
    }

    .pillar h3 {
      font-size: clamp(38px, 9vw, 58px);
    }
  }

  @media (max-width: 620px) {
    .pillars {
      padding: 56px 0;
    }

    .pillars__inner {
      width: calc(100% - 32px);
    }

    .pillars__grid {
      grid-template-columns: 1fr;
    }

    .pillar p {
      max-width: none;
    }
  }
</style>